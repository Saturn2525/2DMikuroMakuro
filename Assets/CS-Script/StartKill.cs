using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartKill : MonoBehaviour
{
    // Start is called before the first frame update
    public void Startkill()
    {
        this.gameObject.SetActive(false);
    }
}
